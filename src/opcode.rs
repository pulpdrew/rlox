#[derive(Debug, PartialEq, Clone, Copy)]
pub enum OpCode {
    Constant(usize),
    Return,
    Add,
    Subtract,
    Multiply,
    Divide,
    Negate,
    Less,
    Greater,
    LessEqual,
    GreaterEqual,
    Not,
    Equal,
    NotEqual,
    Print,
    Pop,
    DeclareGlobal(usize),
    GetGlobal(usize),
    SetGlobal(usize),
    GetLocal(usize),
    SetLocal(usize),
    GetSuper(usize),
    Jump(usize),
    JumpIfTrue(usize),
    JumpIfFalse(usize),
    Invoke(usize),
    Closure(usize),
    GetUpvalue(usize),
    SetUpvalue(usize),
    ReadField(usize),
    SetField(usize),
    Method,
    Inherit,
}
